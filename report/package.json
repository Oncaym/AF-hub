{
  "name": "af-hub-report",
  "version": "1.0.0",
  "private": true,
  "description": "Scheduled reporter: reads each tracker's state and writes its summary to the hub.",
  "main": "report.js",
  "scripts": { "start": "node report.js" },
  "dependencies": { "firebase-admin": "^12.7.0" }
}
